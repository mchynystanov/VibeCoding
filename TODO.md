# Открытые вопросы (раздел 17 ТЗ) — нужно решить до запуска

Владелец ещё не предоставил финальные значения. Ниже — где именно в коде
стоит временная заглушка/плейсхолдер по каждому вопросу.

1. **Telegram-юзернейм/номер** для `TELEGRAM_CHAT_ID` и резервной ссылки —
   `.env.example` (переменные `TELEGRAM_CHAT_ID`, `NEXT_PUBLIC_TELEGRAM_FALLBACK`).
2. **Статус продавца** (официальный дистрибьютор Paomma в КР/КЗ или
   независимый продавец) — влияет на формулировки гарантии в
   `src/components/Footer.tsx` и блоке «Свободные руки» в `src/app/page.tsx`.
3. **Гарантийный сервис на месте** (как обрабатывается гарантийный случай в
   Бишкеке) — `src/components/Faq.tsx` (вопрос №6).
4. **Стоимость и сроки доставки** по Бишкеку / Чуйской области / регионам —
   `src/components/DeliveryAndOrder.tsx`.
5. **Размер скидки на комплект** (молокоотсос + стерилизатор) — по умолчанию
   7% в `src/lib/pricing.ts`, ТЗ предлагает диапазон 5–10%.
6. **Юр. реквизиты** (ИП/самозанятый/др.) и **политика возврата** —
   `src/components/Footer.tsx`.

## Дополнительно

- **Валюта — решено**: Сом (KGS), не ₽. Но: реальная цена в Сом подтверждена
  только для `electric-pump` (2700 Сом, из образца владельца). Цены
  `sterilizer` (4144) и `bionic-pump` (4400) — это старые рублёвые цифры из
  прайса поставщика, у которых просто заменена подпись валюты на "Сом" —
  **фактическая величина не проверена и может быть в разы неверной**.
  Отмечено `// TODO(owner)` прямо в `src/data/products.ts`. Нужно подтвердить
  реальные цены в Сом для этих двух товаров до запуска — иначе на сайте
  будет неправильная цена.
- **Формат каталога изменён**: вместо сетки "Хиты продаж" — один блок на
  товар (баннер + описание), см. `ProductDetailSection` в `src/app/page.tsx`.
  Электрический молокоотсос (`electric-pump`, переименован в "Беспроводной
  молокоотсос 3 в 1" с полными характеристиками от владельца) вернулся в
  этом новом формате.
- **Страница отдельного товара** (`/products/[id]`,
  `src/app/products/[id]/page.tsx`) — для `electric-pump` наполнена по
  образцу `https://paomma.ru/catalog/molokootsos/` (обзор режимов, полная
  таблица характеристик, габариты и комплектация, FAQ) — см. новые поля в
  `src/data/products.ts` (`modes`, `fullSpecsTable`, `packageDimensions`,
  `packageContents`, `faq`) и компоненты `ProductModes.tsx`,
  `ProductSpecsTable.tsx`, `ProductPackage.tsx`, `ProductFaq.tsx`. Разделы
  образца, состоящие только из фото/видео (фотогалерея с наложенными
  карточками, диаграмма мощности, видеообзор, "клуб привилегий",
  чужой футер) сознательно не перенесены — нет исходных материалов и/или
  не относится к нашему магазину.
  - **TODO(owner)**: 5 вопросов FAQ на этой странице настоящие (с образца),
    но ответы — плейсхолдер "Уточняется у производителя, свяжитесь с нами"
    (на сайте-образце ответы скрыты в аккордеоне, текста не видно). Нужны
    реальные ответы от владельца.
  - Для `sterilizer` и `bionic-pump` аналогичное наполнение появится, когда
    будут такие же исходные данные (полные характеристики, габариты
    упаковки, комплектация, реальные вопросы) — пока эти товары используют
    только базовые `specs`.
  - **По решению владельца страница `/products/electric-pump` временно
    показывает только видеообзор** (Kinescope, `product.videoId`) и кнопку
    "Инструкция" (`product.instructionUrl`, PDF) — см. `ProductVideo.tsx`.
    Все описанные выше секции (режимы, характеристики, комплектация, FAQ)
    остаются в коде и коде продукта, но не рендерятся, пока `videoId` задан
    (см. ранний `return` в `ProductDetailClient.tsx`). Для `sterilizer` и
    `bionic-pump` `videoId` не задан — у них показывается обычная страница.
- **Логотип** — реальный файл владельца (`public/logo-paomma.jpg`), в
  `Header.tsx` наложен через `mix-blend-multiply`, чтобы белый фон файла не
  отличался от фона сайта.

- **Реальные фото товаров** — все три товара теперь имеют реальные
  промо-баннеры (инфографика: лого, название, буллеты) в `public/products/`:
  `electric-pump-3v1.png`, `bionic-pump-banner.png`, `sterilizer.png`.
- **`electric-pump` на странице `/products/electric-pump`**: промо-баннер
  `electric-pump-3v1.png` (с вшитым текстом/иконками) используется только на
  главной (`ProductDetailSection`) — на детальной странице он дублировал
  соседний список характеристик (те же факты дважды: один раз иконками на
  картинке, второй раз обычным текстом), поэтому там временно плейсхолдер.
  Владелец пришлёт: (1) "чистое" фото товара без текста — положить в новое
  поле `detailImages` в `src/data/products.ts`; (2) фото руки на приборе для
  секции "Обзор режимов" — разбить на 2 блока (фото | карточки режимов) для
  нормального отображения на телефоне, см. `ProductModes.tsx`.
- **`/products/bionic-pump` переделан по визуалу
  `https://shop.paomma.ru/catalog/molokootsosy/bionic-4-in-1`**: галерея
  фото с миниатюрами (`ProductGallery.tsx`, 10 реальных фото владельца в
  `public/products/bionic-pump/`) + вкладки "О товаре"/"Характеристики и
  состав"/"Инструкция"/"Отзывы" (`ProductTabs.tsx`). Этот же макет теперь
  используют все товары без `videoId` (сейчас — `sterilizer`, у него
  просто нет доп. фото/текста, поэтому миниатюр и `aboutText` не видно).
  - **Сознательно не перенесено с образца**: реальные 194 отзыва на
    образце принадлежат покупателям ДРУГОГО магазина (shop.paomma.ru) —
    вместо копирования добавлена честная ссылка-цитата на них
    (`product.externalReviews`) и **собственная форма отзывов**
    (`ProductReviews.tsx`, `POST /api/reviews`, хранение в
    `data/reviews.json` — гитигнорится, живёт на диске сервера, см.
    `src/lib/reviews.ts`) — отзывы настоящих посетителей нашего сайта,
    публикуются сразу (без модерации), защита от спама — rate-limit +
    honeypot, как в `/api/order`. Цена показана без зачёркнутой "старой"
    цены/скидки/промо ("Скидка РАОММА", "3% с покупки") — это акции того
    магазина, у нас их нет. Шапка магазина (поиск/заказы/избранное/
    логин) и цветовые/возрастные селекторы-варианты не перенесены — у
    нас нет ни аккаунтов, ни нескольких вариантов товара.
  - **Сделано и для `sterilizer`** — тот же макет заполнен реальными
    данными с официальной страницы `https://shop.paomma.ru/catalog/
aksessuary-dlya-butylochek/bottle-sterilizer-warmer` (там пока
    0 отзывов, поэтому `externalReviews` для этого товара не заполнен —
    цитировать нечего) + 13 реальных фото владельца в
    `public/products/sterilizer-gallery/`. `electric-pump` остаётся на
    видео-режиме (`videoId` задан) и этот макет не использует.
- **Админка `/admin`** (цена/наличие товаров) больше не использует
  `.env`/sops — логин и пароль задаются владельцем прямо на `/admin` при
  первом заходе (пока пароль не задан, страница открыта без входа и
  показывает форму "Установите логин и пароль") и хранятся в
  `data/admin-credentials.json` (гитигнорится, живёт на диске сервера,
  см. `src/lib/adminAuth.ts`). Сменить пароль можно там же, в разделе
  "Сменить пароль" внизу страницы (требует текущий пароль). Причина
  отказа от `.env`: перенос ADMIN_* переменных через sops трижды
  блокировался permission-классификатором Claude Code без возможности
  подтверждения.
- **Распродажа**: в `/admin` у каждого товара появилось поле "Скидка на
  распродажу (%)" (0 — без скидки). При скидке >0 на сайте показывается
  зачёркнутая обычная цена + акционная (крупным акцентным шрифтом) +
  бейдж "−N%" — на главной (`ProductDetailSection.tsx`), на странице
  товара (`ProductDetailClient.tsx`) и в результате квиза (`Quiz.tsx`).
  Скидка считается в `src/lib/pricing.ts` (`getSalePrice`, округление до
  целого сома) и хранится в `data/product-overrides.json` наравне с
  ценой/наличием.
  - **Попутно исправлена реальная ошибка**: корзина (`cart-store.ts`)
    раньше брала цену товара из статического `src/data/products.ts`
    напрямую, а не из `data/product-overrides.json` — значит, при смене
    цены/скидки в админке в корзину и в заказ всё равно уходила старая
    цена. Теперь `addItem` принимает уже готовую (актуальную) цену от
    вызывающего компонента, а не подставляет её сама; `Quiz.tsx` для
    этого получил собственный запрос к новому `GET /api/products`
    (публичный, отдаёт `getProducts()` с учётом админки) вместо
    статического импорта каталога.
  - **Обратный отсчёт**: в `/admin` рядом со скидкой можно задать "Распродажа
    до" (дата и время, необязательно). Пока время не вышло — под ценой
    тикающий отсчёт (`SaleCountdown.tsx`, обновляется раз в секунду). Как
    только время проходит, скидка перестаёт действовать сама (проверка в
    `isSaleActive()`, `src/lib/pricing.ts`) — товар просто показывает обычную
    цену, даже если `salePercent` в файле всё ещё больше нуля. Без даты
    окончания скидка действует, пока её не уберут в админке вручную.
    Проверено: и текущий тикающий отсчёт, и автоматическое отключение скидки
    после прошедшей даты — оба ведут себя верно.
- **Счётчик аналитики** (Яндекс.Метрика и/или GA4, раздел 14) — конкретный
  счётчик не выбран. События-заглушки уже расставлены в коде
  (`src/lib/analytics.ts`, вызывается из `cart-store.ts`, `Quiz.tsx`,
  `OrderForm.tsx`) — как появится ID, подключить реальный скрипт в
  `src/app/layout.tsx` и заменить `console.debug` на `ym()`/`gtag()`.
